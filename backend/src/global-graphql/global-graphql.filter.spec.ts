import { GlobalGraphqlFilter } from './global-graphql.filter';

describe('GlobalGraphqlFilter', () => {
  it('should be defined', () => {
    expect(new GlobalGraphqlFilter()).toBeDefined();
  });
});
