services:
  database:
    image: mariadb:12.0.2
    restart: unless-stopped
    environment:
      - MYSQL_DATABASE=app
      - MYSQL_ROOT_PASSWORD=password
    volumes:
      - gofitshop_db_data_dev:/var/lib/mysql
    networks:
      - gofitshop_network_dev
  redis:
    image: redis:7.4.1-alpine
    restart: unless-stopped
    command: ["redis-server", "--loglevel", "warning"]
    volumes:
      - gofitshop_redis_data_dev:/data
    networks:
      - gofitshop_network_dev
  qdrant:
    image: qdrant/qdrant:latest
    restart: unless-stopped
    volumes:
      - gofitshop_qdrant_data_dev:/qdrant/storage
    networks:
      - gofitshop_network_dev
  ollama:
    image: ollama/ollama:latest
    restart: unless-stopped
    volumes:
      - gofitshop_ollama_data_dev:/root/.ollama
    networks:
      - gofitshop_network_dev
    entrypoint: >
      sh -c "
      ollama serve &
      sleep 5 &&
      ollama pull mistral:7b &&
      ollama pull qwen3-embedding:4b &&
      wait
      "
  api:
    image: node:22-bookworm
    working_dir: /app
    volumes:
      - .:/app
      - gofitshop_public_images_data_dev:/app/public/images
    networks:
      - gofitshop_network_dev
    command: sh -c "NODE_ENV=development npm run dev"
    depends_on:
      - database
      - ollama
      - redis
      - qdrant
  nextjs:
    image: node:22-bookworm
    working_dir: /app
    volumes:
      - ../web:/app
    command: sh -c "NODE_ENV=development npm run dev"
    networks:
      - gofitshop_network_dev
    depends_on:
      - api
  stripe-cli:
    image: stripe/stripe-cli:v1.35.0
    env_file:
      - ./.env
    command: "listen --api-key ${STRIPE_API_KEY} --device-name ${STRIPE_DEVICE_NAME} --forward-to http://api:8080/orders/webhook"
    restart: unless-stopped
    networks:
      - gofitshop_network_dev
  caddy:
    image: caddy:2.11-alpine
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./conf:/etc/caddy
    networks:
      - gofitshop_network_dev

networks:
  gofitshop_network_dev:
volumes:
  gofitshop_db_data_dev:
  gofitshop_redis_data_dev:
  gofitshop_qdrant_data_dev:
  gofitshop_ollama_data_dev:
  gofitshop_public_images_data_dev:
