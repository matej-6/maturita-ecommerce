{
  "validation": {
    "required": "{property} is required",
    "minLength": "{property} must be at least {constraints.0} characters long",
    "maxLength": "{property} must be less than {constraints.0} characters long",
    "match": "{property} must match {message} exactly",
    "invalid": "{property} is invalid",
    "field": {
      "confirmPassword": {
        "match": "Confirm password must match password exactly"
      },
      "translation": {
        "minLength": "At least {constraints.0} translation/s is/are required",
        "englishRequired": "English translation is required"
      }
    }
  }
}
